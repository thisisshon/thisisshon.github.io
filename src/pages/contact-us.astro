---
/**
 * Contact Us / Support hub - /contact-us/
 * Tabbed page (Customer Care / Branch Locator / Downloads) with a sticky tab
 * bar, a schedule-a-call lead-capture form and an interactive branch map.
 * Pixel parity with legacy support/index.html.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Breadcrumb from '../components/site/Breadcrumb.astro';

const seo = {
  title: 'Contact & Customer Support',
  description:
    'Get in touch with Shriram Financial Services for account help, trading queries or a free discovery call with a dedicated wealth advisor.',
  path: '/contact-us/',
  // Legacy inline ContactPoint block - ported verbatim (NOT the seo.ts
  // organizationSchema helper, which emits a different FinancialService shape).
  jsonLd: [
    {
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'Shriram Financial Services',
      url: 'https://www.shriramsec.in/',
      contactPoint: [
        {
          '@type': 'ContactPoint',
          telephone: '+91-1800-123-4567',
          contactType: 'customer support',
          areaServed: 'IN',
          availableLanguage: ['en', 'hi'],
          email: 'support@shriramfs.in',
        },
      ],
    },
  ],
};
---

<BaseLayout seo={seo} mainClass="rev-rise-scale">
  <!-- ================= HERO ================= -->
  <section class="hero" id="sec-hero" data-section="hero">
    <Breadcrumb trail={[{ name: 'Contact Us' }]} />
    <div class="hero-grid">
      <div class="hero-inner" data-enter>
        <h1>How Can We <span class="g">Help You?</span></h1>
        <p class="lead">Reach our support team across multiple channels. We're here Monday to Saturday, 9 AM – 6 PM IST.</p>
        <div class="hero-contacts">
          <a class="ct-pill" href="tel:18001234567">
            <svg viewBox="0 0 24 24" fill="none"><path d="M5 4h3l1.5 5-2 1.5a11 11 0 0 0 5 5l1.5-2 5 1.5v3a2 2 0 0 1-2 2A16 16 0 0 1 3 6a2 2 0 0 1 2-2Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path></svg>
            1800 1234567 (Toll Free)
          </a>
          <a class="ct-pill" href="mailto:support@shriramfs.in">
            <svg viewBox="0 0 24 24" fill="none"><rect x="3" y="5" width="18" height="14" rx="2" stroke="currentColor" stroke-width="1.6"></rect><path d="m4 7 8 6 8-6" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path></svg>
            support@shriramfs.in
          </a>
        </div>
      </div>

      <!-- schedule-a-call lead-capture card -->
      <form class="hero-form hero-aside reveal" aria-label="Schedule a call with us" onsubmit="return false;">
        <div class="hf-head">
          <h2>Schedule a Call With Us</h2>
          <p>Drop in your details and an Advisor will reach out shortly:</p>
        </div>
        <div class="hf-body">
          <div class="hf-row">
            <div class="hf-unit">
              <div class="hf-field">
                <div class="hf-field-in">
                  <label for="cf-mobile">Mobile Number</label>
                  <input id="cf-mobile" name="mobile" type="tel" inputmode="numeric" autocomplete="tel-national" placeholder="+91 00000 00000" aria-describedby="err-mobile" required />
                </div>
              </div>
              <p class="hf-err" id="err-mobile">Enter a valid 10-digit mobile number.</p>
            </div>
            <div class="hf-unit">
              <div class="hf-field sel">
                <div class="hf-field-in">
                  <label for="cf-city">City</label>
                  <select id="cf-city" name="city" aria-describedby="err-city" required>
                    <option value="">Select city</option>
                    <option>Bengaluru</option>
                    <option>Chennai</option>
                    <option>Coimbatore</option>
                    <option>Hyderabad</option>
                    <option>Kolkata</option>
                    <option>Madurai</option>
                    <option>Mumbai</option>
                    <option>New Delhi</option>
                    <option>Surat</option>
                    <option>Trichy</option>
                    <option>Vijayawada</option>
                    <option>Visakhapatnam</option>
                    <option>Other</option>
                  </select>
                </div>
              </div>
              <p class="hf-err" id="err-city">Please select your city.</p>
            </div>
          </div>
          <div class="hf-unit">
            <div class="hf-field">
              <div class="hf-field-in">
                <label for="cf-name">Full Name</label>
                <input id="cf-name" name="name" type="text" autocomplete="name" placeholder="Your name" aria-describedby="err-name" required />
              </div>
            </div>
            <p class="hf-err" id="err-name">Please enter your name.</p>
          </div>
        </div>
        <p class="hf-note">By continuing, you agree to be contacted via call, SMS or WhatsApp about your request.</p>
        <button class="btn-gold" type="submit">Continue</button>
        <div class="hf-success" role="status" aria-live="polite">
          <span class="ok" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><path d="m5 13 4 4L19 7" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"></path></svg></span>
          <h3>Request received</h3>
          <p class="hf-msg">Thanks - an advisor will reach out to you shortly.</p>
        </div>
      </form>
    </div>
  </section>

  <!-- ================= TAB BAR ================= -->
  <div class="tabbar">
    <div class="tabbar-inner" role="tablist" aria-label="Contact options">
      <button class="tab" id="tab-care" role="tab" aria-selected="true" aria-controls="panel-care" data-label="Customer Care">Customer Care</button>
      <button class="tab" id="tab-branch" role="tab" aria-selected="false" aria-controls="panel-branch" tabindex="-1" data-label="Branch Locator">Branch Locator</button>
      <button class="tab" id="tab-downloads" role="tab" aria-selected="false" aria-controls="panel-downloads" tabindex="-1" data-label="Downloads">Downloads</button>
    </div>
  </div>

  <!-- ================= PANEL: CUSTOMER CARE ================= -->
  <section class="section stack tabpanel sec-light" id="panel-care" role="tabpanel" aria-labelledby="tab-care"
           data-section="care" style="gap:40px" data-enter="2">
    <div class="stack reveal" style="gap:16px">
      <h2 class="sub-title">Get in Touch</h2>
      <p class="sub-lead">Choose the channel that works best for you.</p>
    </div>
    <div class="hours-bar reveal">
      <svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.6"></circle><path d="M12 7v5l3.5 2" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"></path></svg>
      Support hours: Monday – Saturday, 9:00 AM to 6:00 PM IST. Closed on public holidays.
    </div>
    <div class="chan-grid reveal" data-reveal-row>
      <div class="chan">
        <span class="chan-ico" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><path d="M5 4h3l1.5 5-2 1.5a11 11 0 0 0 5 5l1.5-2 5 1.5v3a2 2 0 0 1-2 2A16 16 0 0 1 3 6a2 2 0 0 1 2-2Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path></svg></span>
        <h3>Toll-free helpline</h3>
        <span class="val"><a href="tel:18001234567">1800 123 4567</a></span>
        <p>No charges from any network across India.</p>
      </div>
      <div class="chan">
        <span class="chan-ico" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><path d="M5 4h3l1.5 5-2 1.5a11 11 0 0 0 5 5l1.5-2 5 1.5v3a2 2 0 0 1-2 2A16 16 0 0 1 3 6a2 2 0 0 1 2-2Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path></svg></span>
        <h3>Alternate number</h3>
        <span class="val"><a href="tel:08046600300">080 4660 0300</a></span>
        <p>Direct line to our Bengaluru head office.</p>
      </div>
      <div class="chan">
        <span class="chan-ico" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><path d="M12 3a9 9 0 0 0-7.7 13.6L3 21l4.5-1.2A9 9 0 1 0 12 3Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path><path d="M9 9.5c0 3 2.5 5.5 5.5 5.5.6 0 1-.5 1-1l-1.6-1-1 .8c-1-.5-1.9-1.4-2.4-2.4l.8-1-1-1.6c-.5 0-1 .4-1 1Z" fill="currentColor"></path></svg></span>
        <h3>WhatsApp chat</h3>
        <span class="val"><a href="https://wa.me/919000000000" target="_blank" rel="noopener">+91 90000 00000</a></span>
        <p>Message us for quick account help.</p>
      </div>
      <div class="chan">
        <span class="chan-ico" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><rect x="3" y="5" width="18" height="14" rx="2" stroke="currentColor" stroke-width="1.6"></rect><path d="m4 7 8 6 8-6" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path></svg></span>
        <h3>Email support</h3>
        <span class="val"><a href="mailto:support@shriramfs.in">support@shriramfs.in</a></span>
        <p>We respond within 24 hours on business days.</p>
      </div>
    </div>
    <div class="office reveal">
      <span class="office-ico" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none"><path d="M4 20V8l8-4 8 4v12" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"></path><path d="M4 20h16M9 20v-5h6v5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"></path><path d="M9 11h.01M15 11h.01" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"></path></svg></span>
      <div class="office-tx">
        <h3>Head office</h3>
        <p>No. 3/1, Residency Cross Rd, Bengaluru – 560025<br/>Mon–Sat, 9 AM – 6 PM</p>
      </div>
    </div>
  </section>

  <!-- ================= PANEL: BRANCH LOCATOR ================= -->
  <section class="section stack tabpanel sec-light" id="panel-branch" role="tabpanel" aria-labelledby="tab-branch"
           data-section="branch" hidden style="gap:40px">
    <div class="stack reveal" style="gap:16px">
      <h2 class="sub-title">Find a Branch Near You</h2>
      <p class="sub-lead">Pick a state and branch - the map jumps to that office.</p>
    </div>
    <div class="branch-search reveal">
      <div class="bs-field">
        <label class="sr-only" for="bs-state">State</label>
        <select id="bs-state" aria-label="State"></select>
      </div>
      <div class="bs-field">
        <label class="sr-only" for="bs-branch">Branch</label>
        <select id="bs-branch" aria-label="Branch"></select>
      </div>
    </div>
    <div class="map-wrap reveal" id="map-wrap">
      <iframe id="branch-map" title="Map of all Shriram Financial Services branches across India" loading="lazy" referrerpolicy="no-referrer-when-downgrade"
        src="https://maps.google.com/maps?q=Shriram%20Financial%20Services%20Pvt%20Ltd%2C%20India&z=5&output=embed"></iframe>
    </div>

    <div class="stack reveal" style="gap:16px">
      <h2 class="sub-title">All Branches</h2>
      <p class="sub-lead">Shriram Financial Services has 21 branches across India - select one above to locate it on the map.</p>
    </div>
    <div class="branch-groups reveal" id="branch-groups"></div>
  </section>

  <!-- ================= PANEL: DOWNLOADS ================= -->
  <section class="section stack tabpanel sec-light" id="panel-downloads" role="tabpanel" aria-labelledby="tab-downloads"
           data-section="downloads" hidden style="gap:40px">
    <div class="stack reveal" style="gap:16px">
      <h2 class="sub-title">Forms &amp; Documents</h2>
      <p class="sub-lead">Download, fill, and submit at your nearest branch or via email.</p>
    </div>

    <div class="doc-toolbar reveal">
      <div class="doc-search">
        <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="1.7"></circle><path d="m20 20-3.2-3.2" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"></path></svg>
        <label class="sr-only" for="doc-q">Search forms</label>
        <input id="doc-q" type="search" placeholder="Search forms &amp; documents" autocomplete="off" />
      </div>
      <div class="doc-chips" role="group" aria-label="Filter by category">
        <button class="doc-chip" type="button" data-cat="all" aria-pressed="true">All</button>
        <button class="doc-chip" type="button" data-cat="grievance" aria-pressed="false">Grievance</button>
        <button class="doc-chip" type="button" data-cat="account-opening" aria-pressed="false">Account Opening</button>
        <button class="doc-chip" type="button" data-cat="kyc" aria-pressed="false">KYC &amp; Compliance</button>
        <button class="doc-chip" type="button" data-cat="dp-demat" aria-pressed="false">DP / Demat</button>
        <button class="doc-chip" type="button" data-cat="funds" aria-pressed="false">Funds &amp; Settlement</button>
      </div>
    </div>
    <p class="doc-empty" id="doc-empty">No forms match your search. Try a different term or category.</p>

    <div class="doc-group reveal" data-cat="grievance">
      <p class="doc-cat">Grievance</p>
      <div class="doc-grid" data-reveal-row>
        <div class="doc-card">
          <span class="nm">Grievance Redressal Policy</span>
          <span class="upd">Updated Jun 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Complaint Registration Form</span>
          <span class="upd">Updated Jun 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
      </div>
    </div>

    <div class="doc-group reveal" data-cat="account-opening">
      <p class="doc-cat">Account Opening</p>
      <div class="doc-grid" data-reveal-row>
        <div class="doc-card">
          <span class="nm">Account Opening Form - Individual</span>
          <span class="upd">Updated Apr 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Account Opening Form - Non-Individual</span>
          <span class="upd">Updated Apr 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Account Closure Form</span>
          <span class="upd">Updated Mar 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
      </div>
    </div>

    <div class="doc-group reveal" data-cat="kyc">
      <p class="doc-cat">KYC &amp; Compliance</p>
      <div class="doc-grid" data-reveal-row>
        <div class="doc-card">
          <span class="nm">KYC Modification Form</span>
          <span class="upd">Updated Jan 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Nomination Form - DP &amp; Trading</span>
          <span class="upd">Updated Mar 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Power of Attorney - Optional</span>
          <span class="upd">Updated Nov 2024</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
      </div>
    </div>

    <div class="doc-group reveal" data-cat="dp-demat">
      <p class="doc-cat">DP / Demat</p>
      <div class="doc-grid" data-reveal-row>
        <div class="doc-card">
          <span class="nm">DIS (Delivery Instruction Slip)</span>
          <span class="upd">Updated Jan 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Demat Account Statement Request</span>
          <span class="upd">Updated Dec 2024</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
      </div>
    </div>

    <div class="doc-group reveal" data-cat="funds">
      <p class="doc-cat">Funds &amp; Settlement</p>
      <div class="doc-grid" data-reveal-row>
        <div class="doc-card">
          <span class="nm">Bank Account Change Request</span>
          <span class="upd">Updated Feb 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
        <div class="doc-card">
          <span class="nm">Funds Withdrawal Request Form</span>
          <span class="upd">Updated Feb 2025</span>
          <a class="doc-dl" href="#">Download</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ================= FINAL CTA ================= -->
  <section class="cta-sec sec-tint" id="sec-final-cta" data-section="final-cta">
    <div class="cta-box dark reveal">
      <div class="ct-tt">
        <h3>Ready to Invest with India's Trusted Partner?</h3>
        <p class="ct-sub">Open a free Demat account in minutes - zero delivery brokerage, SEBI-registered research and a dedicated advisor all in one place.</p>
      </div>
      <div class="cta-actions">
        <a class="btn-gold" href="/open-demat-account/">Open Demat Account</a>
        <a class="btn-ghost" href="#sec-hero">Talk to an Advisor</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* ===== Contact Us / Support - page-specific components. Values reuse the
     shared design tokens; page-local literals flagged Tier-2. ===== */

  .section,
  .cta-sec {
    scroll-margin-top: 96px;
  }

  /* design-system rule: every font renders at a 1.5 line-height. Buttons, inputs, pills and
     links reset to `normal` by default, so restore 1.5 on them explicitly (page-scoped). */
  .ct-pill, .tab, .hf-field-in input, .hf-field-in select, .bs-field select, .doc-search input,
  .bc-meta a, .bc-map, .directions, .doc-dl, .doc-chip, .btn-gold, .btn-dark, .btn-ghost {
  }

  /* scroll-reveal (progressive enhancement: only hides when JS is on).
     Page-local system driven by the page tab script (.reveal/.in) - separate
     from BaseLayout's data-reveal/.js-reveal chrome reveal. */
  :global(html.js) .reveal {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1), transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }
  :global(html.js) .reveal.in {
    opacity: 1;
    transform: none;
  }

  /* ---------- hero: contact column + "schedule a call" capture form.
     Layout (.hero-grid / .hero-inner / .hero-aside width + mobile stacking) is canonical in
     global.css - only the pills and the form's own look live here. ---------- */
  .hero-contacts {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 32px;
    padding-top: 32px;
    border-top: 1px solid #4f524a; /* page-local hairline on gradient - Tier-2 candidate */
  }
  .ct-pill {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    height: 44px;
    padding: 0 16px;
    border-radius: 999px;
    background: #363731; /* page-local - Tier-2 candidate (near sage-900) */
    border: 1px solid #4b4c47; /* page-local - Tier-2 candidate */
    color: var(--color-white);
    font-weight: 500;
    font-size: 14px;
    transition: background 0.2s ease, border-color 0.2s ease;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .ct-pill:hover {
    background: #43443e; /* page-local - Tier-2 candidate */
    border-color: var(--color-sage-700); /* #6d6e6a (Tier-1: absorbed by sage-700) */
  }
  }
  .ct-pill svg {
    width: 16px;
    height: 16px;
    flex: none;
    color: var(--color-gold-400);
  }

  /* "Schedule a call with us" lead-capture card (width + flex come from .hero-aside).
     .hf-head/.hf-body/.hf-note/.hf-success appear on 2-3 pages - shared-extraction candidate. */
  .hero-form {
    background: var(--color-cream-350); /* #f8f4e8 */
    border: 1px solid var(--color-tan-300); /* #e1dbc6 */
    border-radius: 24px;
    padding: clamp(24px, 3vw, 40px);
    display: flex;
    flex-direction: column;
    gap: 32px;
  }
  .hf-head {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .hero-form h2 {
    font-weight: 500;
    font-size: clamp(22px, 2.1vw, 26px);
    color: var(--color-olive-600); /* #4f583b */
  }
  .hf-head p {
    font-weight: 400;
    font-size: 14px;
    color: var(--color-sage-700); /* #6b6f5e */
  }
  .hf-body {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  /* Input field (.hf-row/.hf-unit/.hf-field/.hf-field-in/.hf-err) is the shared
     canonical component in global.css - see DESIGN-SYSTEM.md §4.15. Not redefined here. */
  .hero-form .btn-gold {
    width: 100%;
    height: 48px;
  }
  .hf-note {
    font-weight: 400;
    font-size: 12px;
    color: var(--color-sage-700); /* #6b6f5e */
  }
  /* success state */
  .hf-success {
    display: none;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 12px;
    padding: 8px 0;
  }
  .hero-form.done .hf-body, .hero-form.done .hf-actions, .hero-form.done .btn-gold,
  .hero-form.done .hf-note {
    display: none;
  }
  .hero-form.done .hf-success {
    display: flex;
  }
  .hf-success .ok {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: var(--color-green-100); /* #daead7 (Tier-1: absorbed by green-100) */
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hf-success .ok svg {
    width: 24px;
    height: 24px;
    color: var(--color-success-strong); /* #067a45 */
  }
  .hf-success h3 {
    font-weight: 600;
    font-size: 20px;
    color: var(--color-olive-900); /* #2e3914 */
  }
  .hf-success p {
    font-weight: 400;
    font-size: 14px;
    color: var(--color-sage-700); /* #6b6f5e */
  }

  /* ---------- tab bar: sticky, pinned to the very top (top:0) on a layer BELOW the nav.
     The bar itself never moves, so it can never float mid-screen. When the nav is showing it
     simply sits on top of the bar; when the nav auto-hides on scroll-down it slides up and out,
     revealing the bar at the top. All the motion comes from the nav's own .35s slide - no
     transform, transition or JS on the bar, and zero chance of a floating gap. ---------- */
  .tabbar {
    background: var(--color-surface-page);
    border-bottom: 1px solid var(--color-border-card);
    position: sticky;
    top: 0;
    z-index: var(--z-sticky-tabs);
  }
  .tabbar-inner {
    display: flex;
    gap: 4px;
    padding: 0 var(--pad);
    overflow-x: auto;
    scrollbar-width: none;
  }
  .tabbar-inner::-webkit-scrollbar {
    display: none;
  }
  .tab {
    appearance: none;
    background: none;
    border: 0;
    cursor: pointer;
    font-family: inherit;
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    white-space: nowrap;
    padding: 20px 20px 16px;
    font-weight: 500;
    font-size: 16px;
    color: var(--color-text-tertiary);
    border-bottom: 2px solid transparent;
    margin-bottom: -1px;
    transition: color 0.2s ease, border-color 0.2s ease;
  }
  /* fixed width: hidden bold ghost reserves the active (semibold) width so the row never
     shifts when the selected tab bolds - see DESIGN-SYSTEM §4.16 (Tabs) */
  .tab::after {
    content: attr(data-label);
    font-weight: 600;
    height: 0;
    overflow: hidden;
    visibility: hidden;
    pointer-events: none;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .tab:hover {
    color: var(--color-text-heading-dark);
  }
  }
  .tab[aria-selected='true'] {
    color: var(--color-text-heading-dark);
    font-weight: 600;
    border-bottom-color: var(--color-gold-400);
  }

  .tabpanel[hidden] {
    display: none;
  }

  /* ---------- Customer Care ---------- */
  .hours-bar {
    display: flex;
    align-items: center;
    gap: 12px;
    background: var(--color-surface-card);
    border: 1px solid var(--color-border-card);
    border-radius: 12px;
    padding: 16px 20px;
    font-weight: 500;
    font-size: 14px;
    color: var(--color-text-secondary);
  }
  .hours-bar svg {
    width: 20px;
    height: 20px;
    flex: none;
    color: var(--color-text-heading-dark);
  }

  .chan-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }
  .chan {
    background: var(--color-surface-card);
    border: 1px solid var(--color-border-card);
    border-radius: 24px;
    padding: var(--container-pad);
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .chan-ico {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: var(--color-gold-100); /* #fff0d7 */
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 4px;
  }
  .chan-ico svg {
    width: 24px;
    height: 24px;
    color: var(--color-text-heading-dark);
  }
  .chan h3 {
    font-weight: 500;
    font-size: 18px;
    color: var(--color-text-primary);
  }
  .chan .val {
    font-weight: 600;
    font-size: 18px;
    color: var(--color-text-heading-dark);
    overflow-wrap: anywhere;
  }
  .chan .val a {
    color: inherit;
  }
  .chan p {
    font-weight: 400;
    font-size: 14px;
    color: var(--color-text-muted);
    margin-top: auto;
  }

  .office {
    display: flex;
    align-items: center;
    gap: 24px;
    background: var(--color-surface-card);
    border: 1px solid var(--color-border-card);
    border-radius: 24px;
    padding: var(--container-pad);
  }
  .office-ico {
    width: 72px;
    height: 72px;
    flex: none;
    border-radius: 16px;
    background: var(--color-gold-100); /* #fff0d7 */
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .office-ico svg {
    width: 24px;
    height: 24px;
    color: var(--color-text-heading-dark);
  }
  .office-tx {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .office-tx h3 {
    font-weight: 500;
    font-size: 18px;
    color: var(--color-text-primary);
  }
  .office-tx p {
    font-weight: 400;
    font-size: 16px;
    color: var(--color-text-tertiary);
  }

  /* ---------- Branch Locator ---------- */
  .branch-search {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    align-items: stretch;
  }
  .bs-field {
    position: relative;
    background: var(--color-surface-card);
    border: 1px solid var(--color-border-card);
    border-radius: 12px;
    height: 56px;
    display: flex;
    align-items: center;
    padding: 0 16px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }
  .bs-field:focus-within {
    border-color: var(--color-gold-400);
  }
  .bs-field select {
    appearance: none;
    border: 0;
    outline: none;
    background: none;
    width: 100%;
    font-family: inherit;
    font-weight: 500;
    font-size: 16px;
    color: var(--color-text-primary);
    cursor: pointer;
  }
  .bs-field::after {
    content: '';
    position: absolute;
    right: 16px;
    top: 50%;
    width: 8px;
    height: 8px;
    border-right: 1.6px solid var(--color-gray-450); /* #8a8a8a */
    border-bottom: 1.6px solid var(--color-gray-450); /* #8a8a8a */
    transform: translateY(-65%) rotate(45deg);
    pointer-events: none;
  }

  /* interactive (draggable) Google map embed */
  .map-wrap {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    border: 1px solid var(--color-border-card);
    height: clamp(320px, 38vw, 460px);
    background: var(--color-surface-section-tint);
    transition: opacity 0.35s ease;
  }
  .map-wrap.loading {
    opacity: 0.4;
  }
  .map-wrap iframe {
    width: 100%;
    height: 100%;
    border: 0;
    display: block;
  }

  /* all-branches: state-grouped card grid */
  .branch-groups {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }
  .branch-group {
    display: flex;
    flex-direction: column;
  }
  .branch-gcat {
    font-weight: 600;
    font-size: 12px;
    letter-spacing: 1.2px; /* eyebrow tracking - kept per notes */
    text-transform: uppercase;
    color: var(--color-text-tertiary);
    padding: 0 0 16px;
  }
  .branch-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    align-items: stretch;
  }
  /* light cards - no whole-card hover; only the buttons/links inside have a subtle hover */
  .branch-card {
    background: var(--color-surface-card);
    border: 1px solid var(--color-border-card);
    border-radius: 20px;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .bc-head {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
  .bc-head h3 {
    font-weight: 600;
    font-size: 18px;
    color: var(--color-text-primary);
  }
  .bc-addr {
    font-weight: 400;
    font-size: 14px;
    color: var(--color-text-tertiary);
    flex: 1 0 auto;
  }
  .bc-meta {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .bc-meta a {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    font-weight: 500;
    color: var(--color-text-secondary);
    overflow-wrap: anywhere;
    transition: color 0.2s ease;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .bc-meta a:hover {
    color: var(--color-text-heading-dark);
  }
  }
  .bc-meta svg {
    width: 16px;
    height: 16px;
    flex: none;
    color: var(--color-text-heading-dark);
  }
  .bc-actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 4px;
  }
  .bc-map {
    appearance: none;
    cursor: pointer;
    background: none;
    font-family: inherit;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    height: 40px;
    padding: 0 16px;
    border: 1px solid var(--color-border-card);
    border-radius: 8px;
    color: var(--color-text-heading-dark);
    font-weight: 500;
    font-size: 14px;
    white-space: nowrap;
    transition: background 0.2s ease, border-color 0.2s ease;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .bc-map:hover {
    background: var(--color-surface-section-tint);
    border-color: var(--color-sage-100); /* #c9c8c0 (Tier-1: absorbed by sage-100) */
  }
  }
  .bc-map svg {
    width: 16px;
    height: 16px;
    color: var(--color-gold-400);
  }
  .directions {
    flex: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    height: 40px;
    padding: 0 16px;
    border: 1px solid var(--color-border-card);
    border-radius: 8px;
    color: var(--color-text-heading-dark);
    font-weight: 500;
    font-size: 14px;
    white-space: nowrap;
    transition: background 0.2s ease, border-color 0.2s ease;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .directions:hover {
    background: var(--color-surface-section-tint);
    border-color: var(--color-sage-100); /* #c9c8c0 (Tier-1: absorbed by sage-100) */
  }
  }
  .directions svg {
    width: 16px;
    height: 16px;
    color: var(--color-gold-400);
  }

  /* ---------- Downloads: category groups, card grid, search + chip toolbar and
     empty state now live in global.css (.doc-group/.doc-cat/.doc-toolbar/.doc-search/
     .doc-chips/.doc-chip/.doc-empty) - shared with /regulatory-documents/investor-charter/. ---------- */

  /* ---------- final CTA (dark, centered - shared with About).
     .btn-ghost base is global now; only the page-scoped hover overrides remain. ---------- */
  .cta-box.dark {
    background: var(--color-olive-950);
  }
  .cta-box h3, .cta-box .ct-sub {
  } /* page override: 1.5 leading per design rule */
  .cta-box.dark .ct-tt {
    color: var(--color-white);
  }
  .cta-box.dark h3 .g {
    color: var(--color-gold-400);
  }
  .cta-box.dark .ct-sub {
    color: var(--color-sage-200); /* #bfc1bc */
  }
  .cta-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    justify-content: center;
  }
  .cta-actions .btn-gold, .cta-actions .btn-ghost {
    transition: filter 0.2s ease, background 0.2s ease, border-color 0.2s ease;
  }
  @media (min-width: 1024px) and (hover: hover) {
    .cta-actions .btn-gold:hover {
    filter: brightness(0.96);
  }
  }
  @media (min-width: 1024px) and (hover: hover) {
    .cta-actions .btn-ghost:hover {
    background: var(--color-charcoal-650); /* #383838 */
    border-color: var(--color-white);
  }
  }

  /* .sr-only lived in the legacy shared styles.css but is NOT in the new
     global.css; it visually hides the branch/downloads field labels, so it is
     kept page-local here for parity. (Shared-extraction candidate.) */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* ---------- responsive (two-column hero stacking is canonical in global.css @980px) ---------- */
  @media (max-width: 1024px) {
    .doc-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .chan-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .branch-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 900px) {
    .branch-search {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 640px) {
    .chan-grid {
      grid-template-columns: 1fr;
    }
    .branch-grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 768px) {
    .office {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }
    .chan .val {
      font-size: 18px;
    }
    .bc-actions {
      flex-direction: column;
      align-items: stretch;
    }
    .bc-map, .bc-actions .directions {
      width: 100%;
      justify-content: center;
    }
    .tab {
      font-size: 14px;
      padding: 16px 12px 12px;
    }
  }
  @media (max-width: 560px) {
    .doc-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  /* ---- Contact page: tab switching + scroll reveal + back-to-top + mobile-number grouping ---- */
  (function () {
    'use strict';

    /* ----- tabs ----- */
    var tabs = Array.prototype.slice.call(document.querySelectorAll('.tab'));
    var panels = {
      'tab-care': 'panel-care',
      'tab-branch': 'panel-branch',
      'tab-downloads': 'panel-downloads',
    };
    var hashToTab = { '#customer-care': 'tab-care', '#branch-locator': 'tab-branch', '#downloads': 'tab-downloads' };
    var tabToHash = { 'tab-care': '#customer-care', 'tab-branch': '#branch-locator', 'tab-downloads': '#downloads' };

    function revealIn(panel) {
      Array.prototype.forEach.call(panel.querySelectorAll('.reveal'), function (el) {
        el.classList.add('in');
      });
      /* BaseLayout's IntersectionObserver can't see a panel while it's display:none - force its
         reveal blocks visible the moment the panel is shown so nothing stays faded out. */
      Array.prototype.forEach.call(panel.querySelectorAll('[data-reveal]'), function (el) {
        el.classList.add('is-visible');
      });
    }

    function selectTab(tab, focus, push) {
      tabs.forEach(function (t) {
        var on = t === tab;
        t.setAttribute('aria-selected', on ? 'true' : 'false');
        t.tabIndex = on ? 0 : -1;
        var p = document.getElementById(panels[t.id]);
        if (p) {
          p.hidden = !on;
          if (on) {
            revealIn(p);
          }
        }
      });
      if (focus) {
        tab.focus();
      }
      if (push && tabToHash[tab.id]) {
        history.replaceState(null, '', tabToHash[tab.id]);
      }
    }

    tabs.forEach(function (tab) {
      tab.addEventListener('click', function () {
        selectTab(tab, false, true);
      });
      tab.addEventListener('keydown', function (e) {
        var i = tabs.indexOf(tab),
          n;
        if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
          n = (i + 1) % tabs.length;
        } else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
          n = (i - 1 + tabs.length) % tabs.length;
        } else if (e.key === 'Home') {
          n = 0;
        } else if (e.key === 'End') {
          n = tabs.length - 1;
        } else {
          return;
        }
        e.preventDefault();
        selectTab(tabs[n], true, true);
      });
    });

    /* deep-link: open the tab named in the URL hash on load */
    var initial = hashToTab[window.location.hash];
    if (initial) {
      var t0 = document.getElementById(initial);
      if (t0) {
        selectTab(t0, false, false);
      }
    }

    /* ----- scroll reveal (neutralised under prefers-reduced-motion in global.css) ----- */
    var reveals = Array.prototype.slice.call(document.querySelectorAll('.reveal'));
    if ('IntersectionObserver' in window) {
      var ro = new IntersectionObserver(
        function (entries, obs) {
          entries.forEach(function (e) {
            if (e.isIntersecting) {
              e.target.classList.add('in');
              obs.unobserve(e.target);
            }
          });
        },
        { rootMargin: '0px 0px -10% 0px', threshold: 0.08 }
      );
      reveals.forEach(function (el) {
        ro.observe(el);
      });
    } else {
      reveals.forEach(function (el) {
        el.classList.add('in');
      });
    }

    /* ----- back-to-top ----- */
    var tt = document.querySelector('.to-top');
    if (tt) {
      window.addEventListener(
        'scroll',
        function () {
          tt.classList.toggle('show', window.pageYOffset > 600);
        },
        { passive: true }
      );
      tt.addEventListener('click', function () {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });
    }

    /* ----- mobile field: +91 prefix, group 10 digits as "##### #####" ----- */
    var mob = document.getElementById('cf-mobile');
    if (mob) {
      mob.addEventListener('input', function () {
        var d = mob.value.replace(/\D/g, '');
        if (d.slice(0, 2) === '91') {
          d = d.slice(2);
        }
        d = d.slice(0, 10);
        mob.value = d ? '+91 ' + (d.length > 5 ? d.slice(0, 5) + ' ' + d.slice(5) : d) : '';
      });
    }

    /* ----- Branch locator: full branch directory drives an interactive (draggable) map ----- */
    var BRANCHES = [
      { id: 'indiranagar', name: 'Bangalore – Indiranagar', state: 'Karnataka', addr: '#415, 1st Floor, 7th Cross, Domlur, Bangalore – 560071', phone: '080-41153138 / 22951020 / 25350963', email: 'indiranagar@shriramfs.in', q: 'Domlur Indiranagar Bangalore 560071' },
      { id: 'malleshwaram', name: 'Bangalore – Malleshwaram', state: 'Karnataka', addr: 'No.8, 1st Floor, Between 3rd & 4th Main, 17th Cross Road, Margosa Road, Malleshwaram, Bangalore – 560003', phone: '080-23446764 / 22951019 / 23469641', email: 'malleshwaram@shriramfs.in', q: 'Malleshwaram 17th Cross Margosa Road Bangalore 560003' },
      { id: 'basavanagudi', name: 'Bangalore – Basavanagudi', state: 'Karnataka', addr: 'No.34, First Floor, Annapoorna Complex, South End Road, Basavanagudi, Bangalore – 560004', phone: '080-22445282 / 26547271 / 26540491', email: 'basavanagudi@shriramfs.in', q: 'South End Road Basavanagudi Bangalore 560004' },
      { id: 'vijayanagar', name: 'Bangalore – Vijayanagar', state: 'Karnataka', addr: 'No.201/E, 2nd Floor, 1st Main Road, 1st Stage, Vijayanagar, Bangalore – 560104', phone: '080-22951026 / 23117202 / 23111026', email: 'vijayanagar@shriramfs.in', q: 'Vijayanagar 1st Stage Bangalore 560104' },
      { id: 'hubli', name: 'Hubli', state: 'Karnataka', addr: 'No.S-1, 2nd Floor, R.R. Mahalaxmi Mansion, Pinto Road, Desai Cross, Hubli – 580020', phone: '0836-2257010 / 2257011 / 2257012', email: 'hubli@shriramfs.in', q: 'Desai Cross Pinto Road Hubli 580020' },
      { id: 'mangalore', name: 'Mangalore', state: 'Karnataka', addr: 'D.No.4/6/548/6(1), 2nd Floor, Pramod Automobiles Building, KRR Road, Karangalpady, Mangalore – 575003', phone: '0824-2411123 / 2411124 / 2422717', email: 'mangalore@shriramfs.in', q: 'Karangalpady KRR Road Mangalore 575003' },
      { id: 'shimoga', name: 'Shimoga', state: 'Karnataka', addr: 'Sree Karthik Plaza, 3rd Floor, Opp. Laxmi Galaxy, Above ICICI Bank, Nehru Road, Durgigudi, Shimoga – 577201', phone: '08182-261002 / 261003 / 261005', email: 'shimoga@shriramfs.in', q: 'Durgigudi Nehru Road Shivamogga 577201' },
      { id: 'chennai', name: 'Chennai – Mylapore', state: 'Tamil Nadu', addr: '3rd Floor, 221, Thiru Vi Ka Salai, Royapettah High Road, Luz, Near Vidhya Mandir School, Mylapore, Chennai – 600004', phone: '044-42983690 / 42983643', email: 'chennai@shriramfs.in', q: 'Royapettah High Road Luz Mylapore Chennai 600004' },
      { id: 'coimbatore', name: 'Coimbatore', state: 'Tamil Nadu', addr: 'No.114/A/1, T.V. Samy Road West, R.S. Puram, Coimbatore – 641002', phone: '0422-4313625', email: 'coimbatore@shriramfs.in', q: 'TV Samy Road West RS Puram Coimbatore 641002' },
      { id: 'madurai', name: 'Madurai', state: 'Tamil Nadu', addr: 'Unit No: B-1, No.16 & 17, KRV Arcade – A R Plaza, Block-H, North Veli Street, Madurai – 625001', phone: '0452-4380935', email: 'madurai@shriramfs.in', q: 'North Veli Street Madurai 625001' },
      { id: 'trichy', name: 'Trichy', state: 'Tamil Nadu', addr: '1st Floor, Shop No.4, "Vignesh Plaza", A-6, Thillai Nagar, Trichy – 620018', phone: '0431-4220235', email: 'trichy@shriramfs.in', q: 'Thillai Nagar Tiruchirappalli 620018' },
      { id: 'somajiguda', name: 'Hyderabad – Somajiguda', state: 'Telangana', addr: '6-3-570/1&2, 2nd Floor, Rock Vista, Rock Dale Compound, Somajiguda, Hyderabad – 500082', phone: '040-45461234', email: 'somajiguda@shriramfs.in', q: 'Somajiguda Rock Dale Hyderabad 500082' },
      { id: 'kukatpally', name: 'Hyderabad – Kukatpally', state: 'Telangana', addr: '#15-29-1000/H-14/1, Flat No.101, PKR Complex, Road No.1, KPHB Colony, Kukatpally, Hyderabad – 500072', phone: '040-40211540 / 40063713', email: 'kukatpally@shriramfs.in', q: 'KPHB Colony Kukatpally Hyderabad 500072' },
      { id: 'vizag', name: 'Visakhapatnam', state: 'Andhra Pradesh', addr: '#47-3-7, GF-1, Roshini Palace, 5th Lane, Dwarka Nagar, Visakhapatnam – 530016', phone: '0891-2540053 / 2712808', email: 'vizag@shriramfs.in', q: 'Dwaraka Nagar Visakhapatnam 530016' },
      { id: 'kurnool', name: 'Kurnool', state: 'Andhra Pradesh', addr: 'Shop No.20 & 21, Bhupal Complex, Kurnool – 518001', phone: '08518-278282 / 227095', email: 'kurnool@shriramfs.in', q: 'Bhupal Complex Kurnool 518001' },
      { id: 'vijayawada', name: 'Vijayawada', state: 'Andhra Pradesh', addr: '1st Floor, Om Square, Plot No.89, Near Gupta Kalyana Mandapam, Vijayawada – 520010', phone: '0866-2490719 / 2490720 / 2490721', email: 'vijayawada@shriramfs.in', q: 'Om Square Vijayawada 520010' },
      { id: 'surat', name: 'Surat', state: 'Gujarat', addr: 'G-16, D Block, International Trade Centre, Majura-Gate Ring Road, Surat – 395002', phone: '0261-4081400 / 401 / 402 / 403', email: 'surat@shriramfs.in', q: 'International Trade Centre Majura Gate Ring Road Surat 395002' },
      { id: 'ghatkopar', name: 'Mumbai – Ghatkopar', state: 'Maharashtra', addr: 'No.409/410/411, 4th Floor, Swastik Disa Business Park, LBS Marg, Behind Vadhani Industrial Estate, Ghatkopar West, Mumbai – 400086', phone: '022-61462900', email: 'ghatkopar@shriramfs.in', q: 'Swastik Disa Business Park LBS Marg Ghatkopar West Mumbai 400086' },
      { id: 'andheri', name: 'Mumbai – Andheri', state: 'Maharashtra', addr: '101-104, A Wing, Dynasty Business Park, J B Nagar, Andheri-Kurla Road, Andheri East, Mumbai – 400059', phone: '022-69032900', email: 'andheri@shriramfs.in', q: 'Dynasty Business Park JB Nagar Andheri East Mumbai 400059' },
      { id: 'connaughtplace', name: 'New Delhi – Connaught Place', state: 'Delhi', addr: 'Flat No.704, 7th Floor, Rohit House, Tolstoy Marg, Connaught Place, New Delhi – 110001', phone: '011-41252100 to 106', email: 'connaughtplace@shriramfs.in', q: 'Rohit House Tolstoy Marg Connaught Place New Delhi 110001' },
      { id: 'kolkata', name: 'Kolkata', state: 'West Bengal', addr: '2, Brabourne Road, Govind Bhavan, 7th Floor, Kolkata – 700001', phone: '033-46013385 / 83', email: 'kolkata@shriramfs.in', q: 'Brabourne Road Govind Bhavan Kolkata 700001' },
    ];

    var stateSel = document.getElementById('bs-state'),
      branchSel = document.getElementById('bs-branch'),
      mapWrap = document.getElementById('map-wrap'),
      mapFrame = document.getElementById('branch-map'),
      groupsEl = document.getElementById('branch-groups');

    if (BRANCHES.length && stateSel && branchSel && mapFrame && groupsEl) {
      var byId = {};
      BRANCHES.forEach(function (b) {
        byId[b.id] = b;
      });
      /* unique states, sorted A–Z */
      var states = [];
      BRANCHES.forEach(function (b) {
        if (states.indexOf(b.state) === -1) {
          states.push(b.state);
        }
      });
      states.sort();

      var PHONE_SVG = '<svg viewBox="0 0 24 24" fill="none"><path d="M5 4h3l1.5 5-2 1.5a11 11 0 0 0 5 5l1.5-2 5 1.5v3a2 2 0 0 1-2 2A16 16 0 0 1 3 6a2 2 0 0 1 2-2Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/></svg>';
      var MAIL_SVG = '<svg viewBox="0 0 24 24" fill="none"><rect x="3" y="5" width="18" height="14" rx="2" stroke="currentColor" stroke-width="1.6"/><path d="m4 7 8 6 8-6" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/></svg>';
      /* "View on map" = location pin; "Directions" = a navigation arrow (distinct icons) */
      var PIN_SVG = '<svg viewBox="0 0 24 24" fill="none"><path d="M12 21s7-6.3 7-11a7 7 0 1 0-14 0c0 4.7 7 11 7 11Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/><circle cx="12" cy="10" r="2.4" stroke="currentColor" stroke-width="1.6"/></svg>';
      var NAV_SVG = '<svg viewBox="0 0 24 24" fill="none"><path d="M21 3 3 10.5l7.2 2.3 2.3 7.2L21 3Z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/></svg>';

      function esc(s) {
        return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
      }
      function telHref(p) {
        return 'tel:' + p.split('/')[0].replace(/[^0-9+]/g, '');
      }

      /* default map view: every branch plotted across India */
      var ALL_SRC = 'https://maps.google.com/maps?q=' + encodeURIComponent('Shriram Financial Services Pvt Ltd, India') + '&z=5&output=embed';
      function showAll() {
        mapFrame.src = ALL_SRC;
        mapFrame.title = 'Map of all Shriram Financial Services branches across India';
      }
      function mapSrc(b) {
        return 'https://maps.google.com/maps?q=' + encodeURIComponent(b.q) + '&z=15&output=embed';
      }
      function zoomTo(b, scrollToMap) {
        mapWrap.classList.add('loading');
        mapFrame.addEventListener('load', function once() {
          mapFrame.removeEventListener('load', once);
          mapWrap.classList.remove('loading');
        });
        mapFrame.src = mapSrc(b);
        mapFrame.title = 'Map of ' + b.name + ' branch';
        if (scrollToMap) {
          mapWrap.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
      }

      /* populate the State dropdown (alphabetical) */
      states.forEach(function (s) {
        var o = document.createElement('option');
        o.value = s;
        o.textContent = s;
        stateSel.appendChild(o);
      });
      /* rebuild the Branch dropdown for a given state */
      function fillBranches(state) {
        branchSel.innerHTML = '';
        BRANCHES.filter(function (b) {
          return b.state === state;
        }).forEach(function (b) {
          var o = document.createElement('option');
          o.value = b.id;
          o.textContent = b.name;
          branchSel.appendChild(o);
        });
      }

      /* render the state-grouped "All Branches" card grid (states A–Z) */
      var html = '';
      states.forEach(function (s) {
        html += '<div class="branch-group"><p class="branch-gcat">' + esc(s) + '</p><div class="branch-grid">';
        BRANCHES.filter(function (b) {
          return b.state === s;
        }).forEach(function (b) {
          html +=
            '<article class="branch-card" data-id="' + b.id + '">' +
            '<div class="bc-head"><h3>' + esc(b.name) + '</h3></div>' +
            '<p class="bc-addr">' + esc(b.addr) + '</p>' +
            '<div class="bc-meta">' +
            '<a href="' + telHref(b.phone) + '">' + PHONE_SVG + esc(b.phone) + '</a>' +
            '<a href="mailto:' + esc(b.email) + '">' + MAIL_SVG + esc(b.email) + '</a>' +
            '</div>' +
            '<div class="bc-actions">' +
            '<button class="bc-map" type="button" data-map="' + b.id + '">' + PIN_SVG + 'View on map</button>' +
            '<a class="directions" href="https://maps.google.com/?q=' + encodeURIComponent(b.q) + '" target="_blank" rel="noopener">' + NAV_SVG + 'Directions</a>' +
            '</div>' +
            '</article>';
        });
        html += '</div></div>';
      });
      groupsEl.innerHTML = html;

      /* wire interactions - selecting jumps the map to that branch (no card highlight) */
      stateSel.addEventListener('change', function () {
        fillBranches(stateSel.value);
        if (branchSel.value) {
          zoomTo(byId[branchSel.value], false);
        }
      });
      branchSel.addEventListener('change', function () {
        zoomTo(byId[branchSel.value], false);
      });
      groupsEl.addEventListener('click', function (e) {
        var btn = e.target.closest && e.target.closest('.bc-map');
        if (btn) {
          var b = byId[btn.getAttribute('data-map')];
          stateSel.value = b.state;
          fillBranches(b.state);
          branchSel.value = b.id;
          zoomTo(b, true);
        }
      });

      /* on load: dropdowns preset to Tamil Nadu / Chennai, map shows every branch */
      stateSel.value = byId.chennai.state;
      fillBranches(byId.chennai.state);
      branchSel.value = 'chennai';
      showAll();
    }

    /* ----- Schedule-a-call: validation + inline errors + success state ----- */
    var form = document.querySelector('.hero-form');
    if (form) {
      var fields = [
        { id: 'cf-mobile', err: 'err-mobile', test: function (v) { return v.replace(/\D/g, '').replace(/^91/, '').length === 10; } },
        { id: 'cf-city', err: 'err-city', test: function (v) { return !!v; } },
        { id: 'cf-name', err: 'err-name', test: function (v) { return v.trim().length >= 2; } },
      ];
      var check = function (f, showErr) {
        var input = document.getElementById(f.id),
          errEl = document.getElementById(f.err);
        var ok = f.test(input.value);
        input.closest('.hf-field').classList.toggle('invalid', showErr && !ok);
        errEl.classList.toggle('show', showErr && !ok);
        input.setAttribute('aria-invalid', showErr && !ok ? 'true' : 'false');
        return ok;
      };
      fields.forEach(function (f) {
        var input = document.getElementById(f.id);
        input.addEventListener('input', function () {
          if (input.closest('.hf-field').classList.contains('invalid')) check(f, true);
        });
        input.addEventListener('change', function () {
          check(f, true);
        });
      });
      form.addEventListener('submit', function (e) {
        e.preventDefault();
        var firstBad = null,
          allOk = true;
        fields.forEach(function (f) {
          var ok = check(f, true);
          if (!ok && !firstBad) {
            firstBad = f;
          }
          if (!ok) {
            allOk = false;
          }
        });
        if (!allOk) {
          if (firstBad) {
            document.getElementById(firstBad.id).focus();
          }
          return;
        }
        var name = (document.getElementById('cf-name').value || '').trim().split(' ')[0];
        var msg = form.querySelector('.hf-msg');
        if (name && msg) {
          msg.textContent = 'Thanks, ' + name + ' - an advisor will reach out to you shortly.';
        }
        form.classList.add('done');
      });
    }

    /* ----- Downloads: file-type/size meta + search + category-chip filtering ----- */
    var docCards = Array.prototype.slice.call(document.querySelectorAll('.doc-card'));
    if (docCards.length) {
      /* placeholder file sizes (real files TBD), stamped as "PDF · size · Updated …" */
      var SIZES = ['240 KB', '180 KB', '320 KB', '310 KB', '150 KB', '120 KB', '210 KB', '95 KB', '140 KB', '160 KB', '110 KB', '130 KB'];
      docCards.forEach(function (card, i) {
        var upd = card.querySelector('.upd');
        if (upd) {
          upd.textContent = 'PDF · ' + (SIZES[i] || '-') + ' · ' + upd.textContent;
        }
        card.setAttribute('data-name', (card.querySelector('.nm') || {}).textContent || '');
      });

      var groups = Array.prototype.slice.call(document.querySelectorAll('.doc-group'));
      var chips = Array.prototype.slice.call(document.querySelectorAll('.doc-chip'));
      var qInput = document.getElementById('doc-q');
      var emptyEl = document.getElementById('doc-empty');
      var activeCat = 'all';

      var applyFilter = function () {
        var q = (qInput.value || '').trim().toLowerCase();
        var anyVisible = false;
        groups.forEach(function (g) {
          var catOk = activeCat === 'all' || g.getAttribute('data-cat') === activeCat;
          var groupHasVisible = false;
          Array.prototype.forEach.call(g.querySelectorAll('.doc-card'), function (card) {
            var nameOk = !q || card.getAttribute('data-name').toLowerCase().indexOf(q) !== -1;
            var show = catOk && nameOk;
            card.classList.toggle('hide', !show);
            if (show) {
              groupHasVisible = true;
            }
          });
          g.classList.toggle('hide', !groupHasVisible);
          if (groupHasVisible) {
            anyVisible = true;
          }
        });
        emptyEl.classList.toggle('show', !anyVisible);
      };
      if (qInput) {
        qInput.addEventListener('input', applyFilter);
      }
      chips.forEach(function (chip) {
        chip.addEventListener('click', function () {
          activeCat = chip.getAttribute('data-cat');
          chips.forEach(function (c) {
            c.setAttribute('aria-pressed', c === chip ? 'true' : 'false');
          });
          applyFilter();
        });
      });
    }
  })();
</script>
